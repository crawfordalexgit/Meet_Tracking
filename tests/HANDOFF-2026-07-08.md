# Session Handoff — 2026-07-08 (continue in new chat)

## Where things stand
- **master** is at **v1.0.188**, ~40 commits ahead of origin (NOT pushed — user hasn't asked to push).
- Main checkout: `C:\Users\alex\OneDrive - Mogwai Consultants\00-Tonbridge Swimming\Scripts\Open Meet Dashboard - Claude`
- Active dev server on **:3000** started from worktree `.claude/worktrees/nice-visvesvaraya-cb4596` (synced to master). Run tests as ONE invocation (`npm run test`), not per-project (per-project spawns a webServer each → EADDRINUSE).
- Test suite lives in `tests/` (unit/api/integrity/e2e/destructive). Full findings: `tests/TEST-FINDINGS-2026-07-08.md`. A `test-engineer` sub-agent exists (`.claude/agents/test-engineer.md`).
- Standing rule: **every manually-found bug gets a regression test** (memory: feedback-manual-findings-to-tests).
- DB is fully repopulated: results 5,219 rows (latest 2026-07-04), attendance 21,722 rows.

## DONE & verified this session
- Headline "swimmer shows no meets": **RESOLVED**. Root cause was F9 (heat/final dedupe blocked by UNIQUE(swimmer_id,meet_id,event)). Scrape now dedupes + persists + reports errors. Swimmer pages show meets (Competition tab).
- Fixed & merged: S1, S2, F1–F4, F8, F9, D1–D5, F11(first attempt), squad `||350`, F13.
- **F13 (peak WA = 0 on /swimmers)**: FIXED & visually verified — registry now shows real peaks (Aarush 335, Alyssa 202…). `swimmers.js` now derives peak from `results.wa_pts` (was querying non-existent `swimmer_pbs.wa_pts`).

## IN FLIGHT — needs finishing (data-consistency-2, branch merged to master)
1. **F11 (squad registry vs detail numbers)** — refixed by making BOTH pages feed all-time data to the shared `computeSquadStats` (removed `squads.js` `.gte('date',startStr)` pre-filters). **Consistent by construction**, but:
   - **NEW PERFORMANCE REGRESSION (high):** `/squads` and `/swimmers` now load ~40s in dev because they pull all 21,722 attendance + ~5,200 results **client-side**. All attendance is within 450d so date-windowing won't help. Real fix = **server-side aggregation** (compute squad/swimmer stats in an API route) OR scope per-squad. This is the main open decision.
   - Rendered-number test `tests/e2e/squad-consistency-rendered.spec.js` (selectors verified correct: `.squad-card`/`.squad-name`, health-value-before-label, stat-pills label-then-value). It + `tests/e2e/swimmers-peak-wa.spec.js` (rewritten robust, body-text based) were RE-RUNNING at handoff time — **check background task `bwim0gykd` output** for pass/fail. If they still time out, it's the slow pages, not correctness.
2. **Verify F11 consistency once directly** (Chrome or the rendered test): load `/squads` note AGE DEVELOPMENT Health/Training/Volume, then `/squad/<AGE DEV id>?period=365`, confirm they now MATCH (before fix: 54/21/67 vs 50/17/55).
3. **Close the fix**: branch `fix/data-consistency-2` is merged; delete it. Task #20 still in_progress.

## OPEN findings (not yet fixed) — in TEST-FINDINGS report
- **Perf (high, new):** registry/swimmers ~40s client-side load (see above).
- **F12:** 25 swimmers have Swim England PBs but no results (scraper matches by exact member_id/name, doesn't use `generateNameAliases`).
- **F10:** dashboard "Update Swim England" button fire-and-forgets the meet-scrape SSE stream (shows "Complete!" early). Real meet scrape lives in Settings → Advanced Seasonal Scrapers.
- **F6:** `sync-scm` cascade-delete risk (ON DELETE CASCADE wipes results if a swimmer is removed).
- **F7:** schema.sql drift (issue_upvotes has no id; swimmer_pbs has no wa_pts; live results has date/splits).
- **UX:** F8 session gate blocks whole render 5–16s on hard loads (amplified by Next dev compile). KPI 0% flash.

## Chrome walkthrough (all 15 screens) — DONE
All screens verified working, no console errors: dashboard (real 40/23/63, 24 qualifiers, no fake numbers), swimmer detail (Competition meets render), squads, squad detail, meets (86 open), meet detail, capacity (7 tools), predictor (empty state), reports, feedback, sandbox, settings. Only bugs found: F11-reopened + F13 (both now addressed).

## Immediate next steps for new chat
1. Read background task `bwim0gykd` result (the 2 rendered tests). If pass → F11 verified; commit test fixes, mark done. If timeout → confirm it's the perf slowness.
2. Decide on the F11 performance regression: server-side aggregation vs accept. Recommend farming a fix session for server-side squad/swimmer stats aggregation (kills both the 40s load AND keeps consistency).
3. Delete branch `fix/data-consistency-2`, close task #20.
4. Ask user re: push to origin, and whether to fix F12/F10/F6.
